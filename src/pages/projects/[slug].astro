---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="项目详情 - 我的温馨博客">
  <div class="container mx-auto px-4 py-12 max-w-4xl">
    <a href="/projects" class="inline-flex items-center gap-2 text-text-muted hover:text-primary-400 transition-smooth mb-8">
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
      </svg>
      返回项目列表
    </a>

    <div id="loading" class="text-center py-12">
      <div class="animate-pulse">正在加载...</div>
    </div>

    <div id="not-found" class="hidden text-center py-12">
      <div class="text-6xl mb-4">😕</div>
      <h2 class="text-2xl font-bold mb-4">未找到该项目</h2>
      <a href="/projects" class="inline-block px-6 py-3 bg-primary-300 text-white rounded-lg hover:bg-primary-400 transition-smooth">返回列表</a>
    </div>

    <article id="project-content" class="hidden">
      <div class="glass-effect rounded-2xl overflow-hidden">
        <!-- 封面 -->
        <div id="project-cover" class="h-64 bg-gradient-to-br from-primary-200 to-primary-300 flex items-center justify-center relative">
          <span id="project-emoji" class="text-9xl"></span>
          <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent"></div>
          <span id="project-status-badge" class="absolute top-6 right-6 px-4 py-2 rounded-full text-sm font-bold text-white"></span>
        </div>

        <!-- 内容 -->
        <div class="p-8">
          <h1 id="project-title" class="text-3xl font-bold mb-4"></h1>
          <p id="project-description" class="text-text-secondary text-lg leading-relaxed mb-6"></p>

          <!-- 技术栈 -->
          <div id="tech-section" class="mb-6">
            <h3 class="text-lg font-bold mb-3">🛠️ 技术栈</h3>
            <div id="tech-list" class="flex flex-wrap gap-2"></div>
          </div>

          <!-- 统计 -->
          <div id="stats-section" class="hidden flex gap-6 mb-6 text-lg">
            <div id="stars-section" class="hidden flex items-center gap-2">
              <span>⭐</span>
              <span id="stars-count" class="font-bold"></span>
              <span class="text-text-muted text-sm">Stars</span>
            </div>
            <div id="forks-section" class="hidden flex items-center gap-2">
              <span>🔱</span>
              <span id="forks-count" class="font-bold"></span>
              <span class="text-text-muted text-sm">Forks</span>
            </div>
          </div>

          <!-- 链接 -->
          <div class="flex flex-wrap gap-4 mb-6">
            <a id="github-link" href="#" target="_blank" class="hidden px-6 py-3 bg-gray-800 text-white rounded-lg hover:bg-gray-900 transition-smooth flex items-center gap-2">
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
              GitHub
            </a>
            <a id="demo-link" href="#" target="_blank" class="hidden px-6 py-3 bg-primary-300 text-white rounded-lg hover:bg-primary-400 transition-smooth flex items-center gap-2">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/></svg>
              在线演示
            </a>
          </div>

          <div class="flex gap-4 border-t pt-6">
            <a id="edit-link" href="#" class="auth-only px-6 py-2 bg-primary-300 text-white rounded-lg hover:bg-primary-400 transition-smooth">编辑</a>
            <button id="delete-btn" class="auth-only px-6 py-2 bg-red-500 text-white rounded-lg hover:bg-red-600 transition-smooth">删除</button>
          </div>
        </div>
      </div>
    </article>
  </div>

  <script>
    // 加载并显示项目详情
    async function loadProject(): Promise<void> {
      const pathParts = window.location.pathname.split('/').filter(Boolean);
      const slug = decodeURIComponent(pathParts[pathParts.length - 1]);

      const loading = document.getElementById('loading');
      const notFound = document.getElementById('not-found');
      const content = document.getElementById('project-content');

      if (!loading || !notFound || !content) return;

      try {
        loading.classList.add('hidden');
        const response = await fetch(`/api/projects/${encodeURIComponent(slug)}`);
        if (response.status === 404) {
          notFound.classList.remove('hidden');
          return;
        }
        if (!response.ok) throw new Error('获取数据失败');

        const project = await response.json();

        content.classList.remove('hidden');

        // 封面和状态
        (document.getElementById('project-emoji')!).textContent = project.coverEmoji || '💻';

        const statusBadge = document.getElementById('project-status-badge')!;
        const status = project.status || 'Stable';
        let statusColor = 'bg-green-500';
        if (status === 'Beta') statusColor = 'bg-blue-500';
        if (status === 'WIP') statusColor = 'bg-yellow-500';
        statusBadge.className = `absolute top-6 right-6 px-4 py-2 rounded-full text-sm font-bold text-white ${statusColor}`;
        statusBadge.textContent = status;

        // 标题和描述
        (document.getElementById('project-title')!).textContent = project.title;
        (document.getElementById('project-description')!).textContent = project.description;

        // 技术栈
        if (project.technologies?.length > 0) {
          const techList = document.getElementById('tech-list')!;
          const colors = ['blue', 'green', 'purple', 'orange', 'cyan', 'pink', 'indigo'];
          project.technologies.forEach((tech: string, index: number) => {
            const color = colors[index % colors.length];
            const span = document.createElement('span');
            span.className = `px-3 py-1 bg-${color}-100 text-${color}-600 rounded-full text-sm font-medium`;
            span.textContent = tech;
            techList.appendChild(span);
          });
        }

        // 统计
        if ((project.stars && project.stars > 0) || (project.forks && project.forks > 0)) {
          const statsSection = document.getElementById('stats-section')!;
          statsSection.classList.remove('hidden');
          if (project.stars && project.stars > 0) {
            const starsSection = document.getElementById('stars-section')!;
            starsSection.classList.remove('hidden');
            (document.getElementById('stars-count')!).textContent = project.stars.toString();
          }
          if (project.forks && project.forks > 0) {
            const forksSection = document.getElementById('forks-section')!;
            forksSection.classList.remove('hidden');
            (document.getElementById('forks-count')!).textContent = project.forks.toString();
          }
        }

        // 链接
        if (project.githubUrl) {
          const githubLink = document.getElementById('github-link')!;
          githubLink.classList.remove('hidden');
          githubLink.setAttribute('href', project.githubUrl);
        }
        if (project.demoUrl) {
          const demoLink = document.getElementById('demo-link')!;
          demoLink.classList.remove('hidden');
          demoLink.setAttribute('href', project.demoUrl);
        }

        (document.getElementById('edit-link')!)!.setAttribute('href', `/projects/${slug}/edit`);
        (document.getElementById('delete-btn')!)!.addEventListener('click', async () => {
          if (confirm('确定要删除这个项目吗？')) {
            try {
              const deleteResponse = await fetch(`/api/projects/${project.id}`, { method: 'DELETE' });
              if (!deleteResponse.ok) throw new Error('删除失败');
              alert('已删除');
              window.location.href = '/projects';
            } catch (error: any) {
              alert('删除失败：' + error.message);
            }
          }
        });
      } catch (error: any) {
        if (loading) {
          loading.innerHTML = `<p class="text-red-500">加载失败：${error.message}</p>`;
        }
      }
    }

    // 初始化
    document.addEventListener('DOMContentLoaded', () => {
      loadProject();
    });
  </script>
</Layout>
