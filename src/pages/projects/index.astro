---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="代码项目 - 我的温馨博客">
  <div class="container mx-auto px-4 py-12">
    <!-- 页面标题 -->
    <section class="text-center mb-12">
      <div class="text-6xl mb-4 animate-float">💻</div>
      <h1 class="text-4xl md:text-5xl font-bold mb-4">代码项目</h1>
      <p class="text-xl text-text-secondary">展示我的编程作品</p>
      <a href="/projects/new" class="inline-block mt-6 px-6 py-3 bg-primary-300 text-white rounded-lg hover:bg-primary-400 transition-smooth font-medium">
        💻 添加项目
      </a>
    </section>

    <!-- 项目卡片 -->
    <section id="projects-list" class="max-w-6xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-8">
      <div class="text-center py-12 col-span-full">
        <div class="animate-pulse">正在加载...</div>
      </div>
    </section>

    <!-- 空状态 -->
    <div id="empty-state" class="hidden text-center py-12">
      <div class="text-6xl mb-4">💻</div>
      <p class="text-xl text-text-muted mb-6">还没有项目，开始添加第一个吧</p>
      <a href="/projects/new" class="inline-block px-6 py-3 bg-primary-300 text-white rounded-lg hover:bg-primary-400 transition-smooth">
        添加项目
      </a>
    </div>
  </div>

  <script>
    // 加载并显示项目
    async function loadProjects(): Promise<void> {
      const container = document.getElementById('projects-list');
      const emptyState = document.getElementById('empty-state');

      if (!container || !emptyState) return;

      try {
        const response = await fetch('/api/projects');
        if (!response.ok) throw new Error('获取数据失败');
        const projects = await response.json();

        container.innerHTML = '';

        if (!projects || projects.length === 0) {
          container.classList.add('hidden');
          emptyState.classList.remove('hidden');
          return;
        }

        emptyState.classList.add('hidden');
        container.classList.remove('hidden');

        projects.forEach((project: any, index: number) => {
          const status = project.status || 'Stable';
          let statusColor = 'bg-green-500';
          if (status === 'Beta') statusColor = 'bg-blue-500';
          if (status === 'WIP') statusColor = 'bg-yellow-500';

          const card = document.createElement('div');
          card.className = 'glass-effect rounded-2xl overflow-hidden shadow-md hover:shadow-xl transition-smooth group animate-slide-up';
          card.style.animationDelay = `${index * 0.1}s`;

          // 技术栈标签
          const colors = ['blue', 'green', 'purple', 'orange', 'cyan', 'pink', 'indigo'];
          const techHtml = project.technologies?.length > 0 ? `
            <div class="flex flex-wrap gap-2 mb-4">
              ${project.technologies.map((tech: string, i: number) => {
                const color = colors[i % colors.length];
                return `<span class="px-3 py-1 bg-${color}-100 text-${color}-600 rounded-full text-xs font-medium">${tech}</span>`;
              }).join('')}
            </div>
          ` : '';

          // 统计信息
          const statsHtml = (project.stars > 0 || project.forks > 0) ? `
            <div class="flex gap-4 text-sm text-text-muted mb-4">
              ${project.stars > 0 ? `
                <span class="flex items-center gap-1">
                  <span>⭐</span>
                  <span class="font-bold">${project.stars}</span>
                </span>
              ` : ''}
              ${project.forks > 0 ? `
                <span class="flex items-center gap-1">
                  <span>🔱</span>
                  <span class="font-bold">${project.forks}</span>
                </span>
              ` : ''}
            </div>
          ` : '';

          // 链接按钮
          const linksHtml = `
            <div class="flex gap-3 mb-4">
              ${project.githubUrl ? `
                <a href="${project.githubUrl}" target="_blank" class="text-text-muted hover:text-primary-400 transition-smooth" title="GitHub">
                  <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
                </a>
              ` : ''}
              ${project.demoUrl ? `
                <a href="${project.demoUrl}" target="_blank" class="text-text-muted hover:text-primary-400 transition-smooth" title="在线演示">
                  <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/></svg>
                </a>
              ` : ''}
            </div>
          `;

          card.innerHTML = `
            <!-- 项目封面 -->
            <div class="h-48 bg-gradient-to-br from-primary-200 to-primary-300 flex items-center justify-center relative overflow-hidden">
              <span class="text-8xl group-hover:scale-110 transition-transform duration-300">${project.coverEmoji || '💻'}</span>
              <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent"></div>
              <span class="absolute top-4 right-4 px-3 py-1 ${statusColor} text-white rounded-full text-xs font-bold">${status}</span>
            </div>

            <!-- 项目信息 -->
            <div class="p-6">
              <a href="/projects/${project.slug}" class="text-xl font-bold mb-2 group-hover:text-primary-400 transition-colors block">${project.title}</a>
              <p class="text-text-muted text-sm mb-4 line-clamp-2">
                ${project.description}
              </p>

              ${techHtml}

              <!-- 统计和链接 -->
              <div class="flex items-center justify-between mb-4">
                ${statsHtml ? `<div>${statsHtml}</div>` : '<div></div>'}
                ${linksHtml ? `<div class="flex gap-3">${linksHtml}</div>` : ''}
              </div>

              <!-- 操作按钮 -->
              <div class="flex gap-3 border-t pt-4">
                <a href="/projects/${project.slug}" class="text-primary-400 hover:text-primary-500 transition-smooth text-sm">查看详情</a>
                <a href="/projects/${project.slug}/edit" class="text-blue-500 hover:text-blue-600 transition-smooth text-sm">编辑</a>
                <button class="delete-btn text-red-500 hover:text-red-600 transition-smooth text-sm" data-id="${project.id}">删除</button>
              </div>
            </div>
          `;

          container.appendChild(card);
        });

        // 添加删除事件监听
        document.querySelectorAll('.delete-btn').forEach(btn => {
          btn.addEventListener('click', async (e) => {
            const id = (e.target as HTMLElement).getAttribute('data-id')!;
            if (confirm('确定要删除这个项目吗？')) {
              try {
                const response = await fetch(`/api/projects/${id}`, { method: 'DELETE' });
                if (!response.ok) throw new Error('删除失败');
                loadProjects();
              } catch (error: any) {
                alert('删除失败：' + error.message);
              }
            }
          });
        });
      } catch (error: any) {
        container.innerHTML = `<p class="text-red-500 text-center py-12 col-span-full">加载失败：${error.message}</p>`;
      }
    }

    // 初始化
    document.addEventListener('DOMContentLoaded', () => {
      loadProjects();
    });
  </script>

  <style>
    .line-clamp-2 {
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
  </style>
</Layout>
