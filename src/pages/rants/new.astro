---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="发布吐槽 - 我的温馨博客">
  <div class="container mx-auto px-4 py-12 max-w-4xl">
    <section class="mb-8">
      <h1 class="text-3xl md:text-4xl font-bold mb-2">💢 发布吐槽</h1>
      <p class="text-text-muted">释放情绪，寻找共鸣</p>
    </section>

    <form id="rant-form" class="glass-effect rounded-2xl p-8 space-y-6">
      <div>
        <label class="block text-sm font-medium mb-2">标题 *</label>
        <input type="text" id="title" required class="w-full px-4 py-2 rounded-lg border border-primary-200 focus:outline-none focus:ring-2 focus:ring-primary-300" placeholder="说说让你生气的事..." />
      </div>

      <div>
        <label class="block text-sm font-medium mb-2">内容 *</label>
        <textarea id="content" rows="6" required class="w-full px-4 py-2 rounded-lg border border-primary-200 focus:outline-none focus:ring-2 focus:ring-primary-300" placeholder="详细说说..."></textarea>
      </div>

      <div>
        <label class="block text-sm font-medium mb-2">愤怒指数 (1-10) *</label>
        <div class="flex items-center gap-4">
          <input type="range" id="anger-level" min="1" max="10" value="5" required class="flex-1" />
          <span id="anger-display" class="text-2xl font-bold w-16 text-center">5</span>
        </div>
        <div class="flex justify-between text-xs text-text-muted mt-2">
          <span>😐 轻微</span>
          <span>😤 中等</span>
          <span>🤬 爆炸</span>
        </div>
      </div>

      <div>
        <label class="block text-sm font-medium mb-2">分类</label>
        <input type="text" id="category" class="w-full px-4 py-2 rounded-lg border border-primary-200 focus:outline-none focus:ring-2 focus:ring-primary-300" placeholder="例如: 工作, 生活, 技术" />
      </div>

      <div class="flex gap-4">
        <button type="submit" class="flex-1 px-6 py-3 bg-red-500 text-white rounded-lg hover:bg-red-600 transition-smooth font-medium">
          发布吐槽
        </button>
        <a href="/rants" class="px-6 py-3 bg-gray-200 text-gray-700 rounded-lg hover:bg-gray-300 transition-smooth font-medium text-center">
          取消
        </a>
      </div>
    </form>
  </div>

  <script>
    async function ensureAuth(): Promise<boolean> {
      try {
        const res = await fetch('/api/auth/check');
        const data = await res.json().catch(() => ({}));
        if (data.authenticated) return true;
      } catch {
        // ignore
      }
      const next = encodeURIComponent(window.location.pathname + window.location.search);
      window.location.href = `/login?next=${next}`;
      return false;
    }

    document.addEventListener('DOMContentLoaded', async () => {
      if (!(await ensureAuth())) return;

      const form = document.getElementById('rant-form');
      const angerLevel = document.getElementById('anger-level');
      const angerDisplay = document.getElementById('anger-display');

      if (!form || !angerLevel || !angerDisplay) return;

      // 更新愤怒指数显示
      angerLevel.addEventListener('input', () => {
        angerDisplay.textContent = angerLevel.value;
      });

      form.addEventListener('submit', async (e) => {
        e.preventDefault();

        const data = {
          title: document.getElementById('title').value,
          content: document.getElementById('content').value,
          angerLevel: parseInt(document.getElementById('anger-level').value),
          category: document.getElementById('category').value,
          reactions: 0
        };

        try {
          const response = await fetch('/api/rants', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(data)
          });
          if (response.status === 401) throw new Error('未授权，请先登录');
          if (!response.ok) throw new Error('发布失败');
          alert('吐槽已发布！');
          window.location.href = '/rants';
        } catch (error) {
          alert('发布失败：' + error.message);
        }
      });
    });
  </script>
</Layout>
